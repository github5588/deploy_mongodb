在此目录下载mongo.tgz 
所有集群必须配置ssh免密登录
执行关闭集群脚本 需要安装 killall命令
使用.sh脚本前根据需要调整里面的参数

Download mongo.tgz in this directory
All clusters must be configured with ssh password-free login
To execute the shutdown script, you need to install the killall command.
Adjust the parameters in the .sh script as needed before using it